services:
  plant-talk-forum:
    build: .
    container_name: plant-talk-forum
    ports:
      - "8080:8080"
    volumes:
      - forum-data:/app/data
      - ./frontend/uploads:/app/frontend/uploads   
    environment:
      - PORT=8080
      - DB_PATH=/app/data/forum.db
    restart: unless-stopped
